const king= {name: 'alok', age : 25, village: 'Rajshahi'};
const keys = Object.keys(king); //only keys
const value = Object.values(king); //only values
const entries = Object.entries(king);  // jora jora
console.log(keys);